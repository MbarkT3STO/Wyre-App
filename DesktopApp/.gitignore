# DesktopApp-specific ignores
# (root .gitignore handles node_modules, dist, logs, .env, OS files, etc.)

# Electron packaged release artifacts
release/

# Electron builder cache
.electron-builder-cache/
