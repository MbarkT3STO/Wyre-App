<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Public Downloads folder — where received files are saved -->
    <external-path name="public_downloads" path="Download/" />
    <!-- App-private external files -->
    <external-files-path name="app_downloads" path="Downloads/" />
    <!-- Cache dir — where picked files are copied before sending -->
    <cache-path name="cache" path="." />
    <files-path name="files" path="." />
</paths>
